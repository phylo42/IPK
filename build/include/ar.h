#ifndef RAPPAS_CPP_PHYML_H
#define RAPPAS_CPP_PHYML_H

#include <string>
#include <unordered_map>
#include <memory>
#include "phylo_tree.h"
#include "alignment.h"
#include "row.h"

namespace xpas
{
    class proba_matrix;

    using artree_label_mapping = std::unordered_map<std::string, std::string>;

    namespace io
    {
        /// Load a posterior probability matrix from a file, generated by either PhyML or RAXML-NG
        xpas::proba_matrix load_ar(const std::string& file_name);

        /// Load a node mapping from file
        artree_label_mapping load_artree_mapping(const std::string& file_name);
    }

    proba_matrix ancestral_reconstruction(const std::string& tree_file, const std::string& alignment_file);
}



#endif